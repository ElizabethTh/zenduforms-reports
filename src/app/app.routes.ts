import { Routes } from '@angular/router';
import { AllReports } from './features/reports/pages/all-reports/all-reports';

export const routes: Routes = [
  {
    path: '',
    component: AllReports,
  },
];
